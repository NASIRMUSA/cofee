var conta = document.getElementById('conta');
 
// conta.style.display = "none";

function container() {
	// body...
	var name = document.getElementById('new').value;
	var Num_bar = document.QuenrySelector('new');

	for (var i = new.length - 1; i >= 0; i++) {
		Num_bar.length=20;
		name.value+Num_bar;
		new[i]
	}
}